import { DefaulNumberNullPipe } from './defaul-number-null.pipe';

describe('DefaulNumberNullPipe', () => {
  it('create an instance', () => {
    const pipe = new DefaulNumberNullPipe();
    expect(pipe).toBeTruthy();
  });
});
